from typing import Dict

def create_job_post(title: str, description: str) -> Dict:
    """Create a new job posting."""
    # TODO: post to job boards
    job_id = "JOB3456"
    return {"job_id": job_id, "title": title}